Compact pill for product trademarks, labels, and status indicators.

```jsx
<Badge variant="solid">Weevi Pay™</Badge>
<Badge variant="mint">Smart Dispatch™</Badge>
<Badge variant="status" dotColor="#09D18C">Live</Badge>
```

Variants: `solid` (emerald), `mint`, `outline`, `dark`, `status` (square-ish, with leading dot). For status, pass `dotColor` (emerald = live, grey = beta, amber = upcoming).
